#---------------------------------------------------------------------------------------------------------------------
# tagging and description section
#---------------------------------------------------------------------------------------------------------------------

# DESCRIPTION
# Sample WeBWorK problem, using PGML
# ENDDESCRIPTION
# Library tagging would go here

#---------------------------------------------------------------------------------------------------------------------
# initialization section
#---------------------------------------------------------------------------------------------------------------------

DOCUMENT();
loadMacros(
	"PGstandard.pl",
	"MathObjects.pl",
	"PGML.pl",
	"proofchecker.pl",
	"parserMultiAnswer.pl"
);
loadMacros('ProofFormula.pl');

loadMacros('ProofRules.pl');

# 'P(cos(5)+1)' is the statement that must be proven
$proof = new ProofChecker('P( cos(5) + 1 )');

# Give a hypothesis of the proof that the student is given to be true
$proof -> given('forall(x, P(x+1) )');

# Define an "axiom" (a justification for statements that appears in the drop-down)

warn($ProofRules::ProofRules{'universal_introduction'});


#$proof -> axiom($ProofRules::ProofRules{'universal_introduction'});
#$proof -> axiom($ProofRules{'universal_elimination'});
#$proof -> axiom($ProofRules{'existential_introduction'});
#$proof -> axiom($ProofRules{'existential_elimination'});


$proof -> show();


ENDDOCUMENT();


